# Full image variant: everything in the slim image plus Markdown conversion and
# NotebookLM uploads. Markdown conversion itself is done by the full pandoc
# build that the Dockerfile installs.
-r requirements.txt

# Auth via NOTEBOOKLM_AUTH_JSON, no browser needed. Requires Python 3.10+.
notebooklm-py>=0.8,<1; python_version >= "3.10"
